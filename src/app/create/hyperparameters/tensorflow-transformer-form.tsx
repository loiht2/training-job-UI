import { createPlaceholderHyperparameterConfig } from "./generic-form";

export const TENSORFLOW_TRANSFORMER_HYPERPARAMETER_CONFIG = createPlaceholderHyperparameterConfig(
  "tensorflow-transformer",
  "TensorFlow Transformer"
);
